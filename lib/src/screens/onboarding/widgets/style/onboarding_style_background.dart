import 'package:flutter/material.dart';

class OnboardingStyleBackground extends StatelessWidget {
  const OnboardingStyleBackground({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox.shrink();
  }
}
